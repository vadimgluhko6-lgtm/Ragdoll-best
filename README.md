
if not game:IsLoaded() then game.Loaded:Wait(); end; local Plrs = game:GetService("Players"); local Dbr = game:GetService("Debris"); local LocalPlayer = Plrs.LocalPlayer; local CORPSE_LIFETIME = 180; local function applyFlawlessRagdoll(char) if not char or char:FindFirstChild("IsDead") then return; end; local hum = char:WaitForChild("Humanoid", 5); local torso = char:WaitForChild("Torso", 5); if not hum or not torso or char:FindFirstChild("UpperTorso") then return; end; local isLocal = (Plrs:GetPlayerFromCharacter(char) == LocalPlayer); hum.Died:Connect(function() if char:FindFirstChild("IsDead") then return; end; Instance.new("BoolValue", char).Name = "IsDead"; hum.BreakJointsOnDeath = false; for index, t in ipairs(char:GetChildren()) do if t:IsA("Tool") then t:Destroy(); end; end; local savedLook = {}; for index, item in ipairs(char:GetChildren()) do if item:IsA("Accessory") and item:FindFirstChild("Handle") then local handle = item.Handle; local mesh = handle:FindFirstChildOfClass("SpecialMesh") or handle:FindFirstChildOfClass("MeshPart"); local oldWeld = handle:FindFirstChildOfClass("Weld") or handle:FindFirstChild("AccessoryWeld") or handle:FindFirstChildOfClass("WeldConstraint"); local targetName = (oldWeld and oldWeld.Part1 and oldWeld.Part1.Name) or "Head"; local itemNameLower = item.Name:lower(); if itemNameLower:find("tail") or itemNameLower:find("wings") or itemNameLower:find("cape") or itemNameLower:find("back") or itemNameLower:find("waist") or itemNameLower:find("belt") or itemNameLower:find("hip") then targetName = "Torso"; end; local targetPart = char:FindFirstChild(targetName) or char:FindFirstChild("Head"); if targetPart then local calculatedWeldOffset = CFrame.new(); if oldWeld and oldWeld:IsA("JointInstance") and oldWeld.Part0 and oldWeld.Part1 then calculatedWeldOffset = oldWeld.C0 * oldWeld.C1:Inverse(); else calculatedWeldOffset = targetPart.CFrame:ToObjectSpace(handle.CFrame); end; if isLocal and (itemNameLower:find("tail") or itemNameLower:find("wings") or itemNameLower:find("cape") or itemNameLower:find("back") or itemNameLower:find("waist") or itemNameLower:find("belt") or itemNameLower:find("hip")) then calculatedWeldOffset = CFrame.new(calculatedWeldOffset.Position); end; table.insert(savedLook, { Name = item.Name, Target = targetName, Handle = handle, C0 = oldWeld and oldWeld:IsA("JointInstance") and oldWeld.C0 or nil, C1 = oldWeld and oldWeld:IsA("JointInstance") and oldWeld.C1 or nil, LocalOffset = targetPart.CFrame:ToObjectSpace(handle.CFrame) }); end; end; end; local armorToClone = {}; local r6Limbs = {["Torso"]=true,["Head"]=true,["Left Arm"]=true,["Right Arm"]=true,["Left Leg"]=true,["Right Leg"]=true}; for index, obj in ipairs(char:GetDescendants()) do if obj:IsA("BasePart") and obj.Name ~= "HumanoidRootPart" and not r6Limbs[obj.Name] then if not obj:FindFirstAncestorOfClass("Accessory") and not obj:FindFirstAncestorOfClass("Tool") then local objNameLower = obj.Name:lower(); if objNameLower:find("helmet") or objNameLower:find("visor") or objNameLower:find("glass") or objNameLower:find("088") then continue; end; local detectedParent = "Torso"; for _, joint in ipairs(char:GetDescendants()) do if joint:IsA("JointInstance") or joint:IsA("WeldConstraint") then if (joint.Part0 == obj and joint.Part1 and r6Limbs[joint.Part1.Name]) then detectedParent = joint.Part1.Name; break; elseif (joint.Part1 == obj and joint.Part0 and r6Limbs[joint.Part0.Name]) then detectedParent = obj.Parent and r6Limbs[obj.Parent.Name] and obj.Parent.Name or "Torso"; break; end; end; end; local limbPart = char:FindFirstChild(detectedParent); local offset = CFrame.new(); if limbPart then offset = limbPart.CFrame:ToObjectSpace(obj.CFrame); end; table.insert(armorToClone, {Obj = obj, TargetName = detectedParent, LocalOffset = offset}); end; end; end; task.wait(0.01); local rag = Instance.new("Model", workspace); rag.Name = "PureRagdollCorpse"; Instance.new("Humanoid", rag).PlatformStand = true; rag.Humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None; for index, o in ipairs(char:GetChildren()) do if o:IsA("Shirt") or o:IsA("Pants") or o:IsA("ShirtGraphic") or o:IsA("BodyColors") then o:Clone().Parent = rag; end; end; local parts = {}; for index, child in ipairs(char:GetChildren()) do if child:IsA("BasePart") and r6Limbs[child.Name] then local p = child:Clone(); p.Parent = rag; p.CanCollide = false; p.CFrame = child.CFrame; p.Velocity = child.Velocity + Vector3.new(math.random(-18, 18), math.random(12, 22), math.random(-18, 18)); p.RotVelocity = child.RotVelocity + Vector3.new(math.random(-12, 12), math.random(-8, 8), math.random(-12, 12)); parts[child.Name] = p; end; end; local rTorso = parts["Torso"]; if not rTorso then rag:Destroy(); return; end; local fakeRoot = Instance.new("Part", rag); fakeRoot.Name = "HumanoidRootPart"; fakeRoot.Size = Vector3.new(2, 2, 1); fakeRoot.Transparency = 1; fakeRoot.CanCollide = false; fakeRoot.CFrame = rTorso.CFrame; Instance.new("WeldConstraint", fakeRoot).Part0, fakeRoot.WeldConstraint.Part1 = rTorso, fakeRoot; local jointsData = { {Part = "Left Arm", Pos = Vector3.new(-1, 0.5, 0), LimbPos = Vector3.new(0.5, 0.5, 0), Rot = true}, {Part = "Right Arm", Pos = Vector3.new(1, 0.5, 0), LimbPos = Vector3.new(-0.5, 0.5, 0), Rot = true}, {Part = "Left Leg", Pos = Vector3.new(-0.5, -1, 0), LimbPos = Vector3.new(0, 1, 0), Rot = true}, {Part = "Right Leg", Pos = Vector3.new(0.5, -1, 0), LimbPos = Vector3.new(0, 1, 0), Rot = true}, {Part = "Head", Pos = Vector3.new(0, 1, 0), LimbPos = Vector3.new(0, -0.5, 0), Rot = false} }; for index, data in ipairs(jointsData) do local rLimb = parts[data.Part]; if rLimb then local a0, a1 = Instance.new("Attachment", rTorso), Instance.new("Attachment", rLimb); if data.Rot then a0.CFrame = CFrame.new(data.Pos) * CFrame.Angles(math.rad(90), 0, 0); a1.CFrame = CFrame.new(data.LimbPos) * CFrame.Angles(math.rad(90), 0, 0); else a0.CFrame = CFrame.new(data.Pos); a1.CFrame = CFrame.new(data.LimbPos); end; local socket = Instance.new("BallSocketConstraint", rLimb); socket.Attachment0 = a0; socket.Attachment1 = a1; socket.LimitsEnabled = false; socket.Parent = rLimb; local noCol = Instance.new("NoCollisionConstraint"); noCol.Part0 = rTorso; noCol.Part1 = rLimb; noCol.Parent = rLimb; end; end; if parts["Left Leg"] and parts["Right Leg"] then local legNoCol = Instance.new("NoCollisionConstraint", rag); legNoCol.Part0 = parts["Left Leg"]; legNoCol.Part1 = parts["Right Leg"]; end; local hitboxes = {}; for partName, limbPart in pairs(parts) do local hitbox = Instance.new("Part", rag); hitbox.Name = partName .. "_ColliderHitbox"; hitbox.Transparency = 1; hitbox.CanCollide = true; if partName == "Torso" then hitbox.Shape, hitbox.Size = Enum.PartType.Block, limbPart.Size - Vector3.new(0.05, 0.05, 0.05); hitbox.CFrame = limbPart.CFrame; elseif partName == "Head" then hitbox.Shape, hitbox.Size = Enum.PartType.Ball, limbPart.Size - Vector3.new(0.3, 0.3, 0.3); hitbox.CFrame = limbPart.CFrame; else hitbox.Shape = Enum.PartType.Cylinder; hitbox.Size = Vector3.new(limbPart.Size.Y - 0.05, math.min(limbPart.Size.X, limbPart.Size.Z) - 0.3, math.min(limbPart.Size.X, limbPart.Size.Z) - 0.3); hitbox.CFrame = limbPart.CFrame * CFrame.Angles(0, 0, math.rad(90)); end; hitbox.CustomPhysicalProperties = PhysicalProperties.new(1.5, 1.2, 0.1, 1.5, 1.5); local hitWeld = Instance.new("WeldConstraint"); hitWeld.Part0 = limbPart; hitWeld.Part1 = hitbox; hitWeld.Parent = hitbox; if partName ~= "Torso" then local noColHit = Instance.new("NoCollisionConstraint"); noColHit.Part0 = rTorso; noColHit.Part1 = hitbox; noColHit.Parent = hitbox; end; hitboxes[partName] = hitbox; end; if hitboxes["Left Leg"] and hitboxes["Right Leg"] then local hitboxLegNoCol = Instance.new("NoCollisionConstraint", rag); hitboxLegNoCol.Part0 = hitboxes["Left Leg"]; hitboxLegNoCol.Part1 = hitboxes["Right Leg"]; end; for index, armor in ipairs(armorToClone) do pcall(function() local origPart = armor.Obj; if origPart and origPart.Parent then local armorPart = origPart:Clone(); local targetLimb = parts[armor.TargetName] or rTorso; if targetLimb then local descList = armorPart:GetDescendants(); local dccLen = #descList; local dccIdx = 1; while dccIdx <= dccLen do local c = descList[dccIdx]; dccIdx = dccIdx + 1; if c and (c:IsA("JointInstance") or c:IsA("WeldConstraint")) then c:Destroy(); end; end; local topW = armorPart:FindFirstChildOfClass("Weld") or armorPart:FindFirstChildOfClass("WeldConstraint"); if topW then topW:Destroy(); end; local w = Instance.new("WeldConstraint", armorPart); local relativePos = armor.LocalOffset and armor.LocalOffset.Position or Vector3.new(0,0,0); armorPart.CFrame = targetLimb.CFrame * CFrame.new(relativePos.X, relativePos.Y, relativePos.Z); w.Part0 = targetLimb; w.Part1 = armorPart; w.Parent = armorPart; armorPart.CanCollide = false; armorPart.Parent = rag; end; end; end); end; if Plrs:GetPlayerFromCharacter(char) == LocalPlayer then task.spawn(function() local cam = workspace.CurrentCamera; local currentSnapshot = LocalPlayer.Character; while LocalPlayer.Character == currentSnapshot do if not rTorso or not rTorso.Parent or not cam then break; end; cam.CameraSubject = rTorso; task.wait(); end; if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then cam.CameraSubject = LocalPlayer.Character.Humanoid; end; end); end; for index, data in ipairs(savedLook) do local targetPart = parts[data.Target] or parts["Head"]; if targetPart and data.Handle and data.Handle.Parent then local hCl = data.Handle:Clone(); hCl.CanCollide, hCl.CanQuery, hCl.CanTouch = false, false, false; local accModel = Instance.new("Accessory", rag); accModel.Name = data.Handle.Parent.Name; hCl.Parent = accModel; if isLocal and data.C0 and data.C1 then local w = Instance.new("Weld", hCl); w.Part0, w.Part1 = targetPart, hCl; w.C0, w.C1 = data.C0, data.C1; w.Parent = hCl; else local finalOffset = data.LocalOffset; if finalOffset then finalOffset = CFrame.new(finalOffset.Position); end; if not finalOffset or (type(finalOffset) == "userdata" and finalOffset.Position.Magnitude > 10) then finalOffset = CFrame.new(0,0,0); end; hCl.CFrame = targetPart.CFrame * finalOffset; local w = Instance.new("WeldConstraint", hCl); w.Part0, w.Part1 = targetPart, hCl; w.Parent = hCl; end; hCl.Parent = accModel; end; end; task.spawn(function() for i = 1, 20 do for index, p in ipairs(rag:GetChildren()) do if p.Name:find("Hitbox") then p.CanCollide = true; end; end; task.wait(0.1); end; end); task.delay(5, function() if rag and rag.Parent then local desc = rag:GetDescendants(); table.foreach(desc, function(index, item) if item:IsA("BasePart") then item.Anchored = true; end; end); task.wait(0.1); table.foreach(desc, function(index, item) if item:IsA("BasePart") then item.CanCollide = false; end; end); end; end); for index, child in ipairs(char:GetChildren()) do if child:IsA("BasePart") then child.Transparency, child.CanCollide = 1, false; elseif child:IsA("Accessory") and child:FindFirstChild("Handle") then child.Handle.Transparency = 1; end; end; task.delay(1, function() if char and char.Parent then char:Destroy(); end; end); dbrTarget = rag; Dbr:AddItem(dbrTarget, CORPSE_LIFETIME); end); end; local function listen(p) p.CharacterAdded:Connect(function(char) if p == LocalPlayer then task.wait(0.2); local cam = workspace.CurrentCamera; local hum = char:WaitForChild("Humanoid", 5); if hum then cam.CameraSubject = hum; end; end; task.spawn(applyFlawlessRagdoll, char); end); if p.Character then if p == LocalPlayer then workspace.CurrentCamera.CameraSubject = p.Character:WaitForChild("Humanoid", 5); end; task.spawn(applyFlawlessRagdoll, p.Character); end; end; Plrs.PlayerAdded:Connect(listen); local currentPlrs = Plrs:GetPlayers(); local plrsIndex = 1; while currentPlrs[plrsIndex] do task.spawn(listen, currentPlrs[plrsIndex]); plrsIndex = plrsIndex + 1; end;
